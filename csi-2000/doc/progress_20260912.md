# CSI2000 进度（2026-09-12 20:57）

写于 2026-09-12。本目录是中证2000 研究树，禁止改
`trading_framework` / `cn_data_bs` / 注册 `TradingSystem-*`。
选参只认 `paper_trader` 8 相位 + 多维指标。Qlib 自带回测和
rolling JSON 里的 hifi 表不算验收。

看板：[csi2000-mission.canvas.tsx](C:/Users/gaoyu/.cursor/projects/c-Users-gaoyu-Desktop-quant/canvases/csi2000-mission.canvas.tsx)

## 收工条件（未完成）

1. `paper_trader` 8 相位稳定有净超额（相对 `932000.CSI`，不是沪深300）
2. 标签 = 成交价 = 下单时钟（路线 B：14:45 出分 + 14:50 收盘竞价）
3. 掘金仿真账户真正建仓（现在仍是空仓）
4. 周一竞价链路跑通

## 当前生产配置

文件：`config/signal_config.yaml`。未过 8 相位多维门不要改。

| 项 | 值 |
|---|---|
| preset | `alpha158_ovn` |
| exec_lag | 0（当日 14:50 竞价） |
| instruments | csi2000 |
| benchmark | 932000.CSI |
| vol_target | 0.25 |
| vol_unknown_exposure | 0.40 |
| topk / n_drop / rebalance | 100 / 20 / 8 |
| min_amount | 200 万 |
| stop_loss | null |
| adaptive_strategy | none |
| 标签 | T 收盘 → T+1 开盘；当日涨停样本置 NaN |

解释器：`C:\Users\gaoyu\.conda\envs\qlib\python.exe -X utf8`。
工作目录：`qlib/csi-2000/`。凭证在 `csi-2000/.env`。
掘金仿真只许 `21761e6d-ad82-11f1-8afe-00163e022aa6`。
数据：`C:\Users\gaoyu\.qlib\qlib_data\cn_data_csi2000`，
日历约 2019-10-08 ~ 2026-09-11。`turn` / `isST` 为 NaN，有 `amount`。

## 已过门的验收数字

> ⚠ 2026-09-12 作废：下表全部跑的是 exec_lag=1（T+1 收盘成交），
> 不是路线 B。原因见文末「代码审计 + 修复」。重跑前不要引用。

主段 2024-01-02 ~ 2026-09-11，`paper_trader`，8 相位。
基准中证2000 同期约 +28.0%。

### 无约束基线

- Sharpe 均值 0.75 / 最差 0.61
- 收益均值 +49.8%
- 最差回撤 **-34.7%**（主要是指数 beta，相关约 0.89）

### vol_target 扫描（已写入 0.25）

| 配置 | Sharpe 均值/最差 | 最差回撤 | Sharpe 配对 p |
|---|---|---|---|
| null | 0.75 / 0.61 | -34.7% | — |
| tv16 | 0.78 / 0.69 | -24.6% | 0.35 |
| tv20 | 0.80 / 0.73 | -26.1% | 0.065 |
| **tv25** | **0.87 / 0.75** | **-26.1%** | **0.003** |

### 开局未知敞口（都在 tv25 上，N=6）

| 开局敞口 | Sharpe | DSR | Sortino | Calmar | IR | 最差回撤 |
|---|---|---|---|---|---|---|
| 0.60 | 0.87 / 0.75 | 0.53 | 1.06 | 0.89 | 0.39 | -26.1% |
| 0.50 | 0.95 / 0.79 | 0.57 | 1.17 | 1.04 | 0.50 | -23.5% |
| **0.40** | **0.98 / 0.79** | **0.59** | **1.21** | **1.13** | **0.53** | **-21.4%** |

0.40 vs 0.60：Sharpe Δ+0.11（p=0.008）。
收益均值约 +68.8%，相对中证2000 超额均值约 +40.8%，最差 +23.3%。

改 yaml 门槛：8 相位配对同时改善 Sharpe 均值+最差、最差回撤，
且 DSR / Sortino / Calmar / IR 不能变差。

## 做过但未过门（不要切生产）

开源量价筛选（WorldQuant 101 / Amihud / VWAP，不是论文挖掘）。
2021–22 拟合、2023 确认，2024 不当选参。12 个因子写入
`factor_lab/results/factor_eval/os_selected.json`。
新预设 `alpha158_ovn_os` 已 9 窗训完。

8 相位 vs 生产（同一套 tv25+0.40）：

- Sharpe 0.96/0.80 vs **0.98/0.79**，配对 p=0.79
- DSR 0.55 vs **0.59**；Calmar 最差 0.71 vs **0.95**
- 最差回撤 **-25.4% vs -21.4%**

结论：和 Alpha158 重叠太多，无稳定增量。yaml 未切。
不要再筛一轮现成 WQ101 交差。

## 2026-09-12 逻辑审计

脚本：`python -m factor_lab.diagnose_horizon`
区间：2024-01-02 ~ 2026-02-05（挖掘评估期，不碰 holdout）。
成交额五分位作微盘代理（供给层无流通市值）。

### 分钟数据：设计是赶在跳空前买，不是躲开隔夜

路线 B 本意：T 日 14:45 出分、14:50 收盘竞价成交，
把 T 收盘 → T+1 开盘的跳空吃下来。标签也是这段隔夜。

历史 15 分钟缓存是空的：

- `C:\Users\gaoyu\.qlib\data_hub\raw\tushare\stk_mins`：不存在
- 截断日线 `daily_trunc_1445`：不存在

所以 14:45 截断重训还做不了。训练仍用完整日线，
和盘中 14:45 特征不同分布。周一盘中预取可以给当天出分铺路，
不能当 Alpha360 全历史训。

### 微盘空间确实更大，但再往更薄拧会伤隔夜

全市场隔夜均值 **-6.8bp**（最厚一档 -40bp 把均值拉负）。
8 日等权 **+93.9bp**，去掉第一晚后的 7 日 **+101.0bp**。

| 成交额档 | 隔夜 | 8 日 |
|---|---|---|
| 最薄 | +7.4bp | +127.6bp |
| Q2 | +3.9bp | +127.1bp |
| Q3 | +0.4bp | +122.8bp |
| Q4 | -5.9bp | +96.1bp |
| 最厚 | -40.0bp | -4.1bp |

`min_amount=200万` 几乎不起滤网：日均约 1963 只里 1963 只过门
（99.98%），最薄档也有 99.9% 过。不是卡住微盘的原因。

### 生产模型的超额几乎全在第一晚

64 次调仓、生产 `alpha158_ovn` Top100：

| 组合 | 隔夜 | 8 日 | 去掉第一晚的 7 日 |
|---|---|---|---|
| 全市场等权 | -6.8bp | +93.9bp | +101.0bp |
| 生产 Top100 | **+44.5bp** | +151.3bp | +106.1bp |
| 分数前 200 再取最薄 100 | +21.9bp | +124.8bp | — |

含义：

- Top100 隔夜超额约 +51bp，后 7 日超额只剩约 +5bp。
- 持仓里 43.8% 已在最薄成交额档（随机应是 20%），最厚档只有 3.9%。
- 在分数前 200 里再挑更薄的 100，隔夜从 +44.5 掉到 +21.9。
  模型已经挑的是「会跳的薄票」，不是任意微盘。
- 现在的错位：标签是隔夜，持仓却是 8 个交易日，后面 7 天在坐指数 beta
  （相关约 0.89，大回撤来自指数急跌）。

## 下一刀优先级（先过 8 相位再写 yaml）

1. **对齐持仓和标签**（杠杆最大）
   - T+1 开盘平仓（真隔夜），或
   - 标签改 8 日前向再训，或
   - 把 `rebalance_every` 缩到 1–2 日
   - 不要继续用隔夜标签却拿满 8 天当「吃微盘」
2. **补全历史 15 分钟 → 14:45 截断日线重训**
   - 训练特征和 14:45 出分同分布
   - 全量约 8.6 小时，`python -m qlib_engine.fetch_minute`
   - 不要和挖掘抢 Tushare 配额（挖掘本身读本地 Qlib）
3. **论文挖掘**（QuantaAlpha / FactorMiner）
   - 评估已改 CSI2000 + 隔夜标签，去掉 `$turn`
   - 挖掘评估期止于 2026-02-05；`MINING_HOLDOUT_START=2026-02-06`
     之后验收不得碰
   - 接受：`python -m factor_lab.factor_miner --accept run_XXX`
     再 8 相位多维门，过了才写 yaml / persist boosters
4. **不要**：再降 `min_amount`、盲目按成交额倾斜、再筛 WQ101

其它未做：掘金建仓 + 周一时钟；universe profile 收口；
`daily._require_csi2000` / GM 账户写死；无分数卖出排序。

## 挖掘链路（进行中）

2026-09-12 20:55 已启动：

```text
python -m factor_lab.factor_miner --daily
```

日志：`logs/factor_miner.log`（`scheduled_log` 会把 stdout 重定向到这里）。
锁：`factor_lab/mining_results/.lock`。本次 run_004，预算 5 小时。
Qlib 已指到 `cn_data_csi2000`。

### 这次动手前改过的 CSI300 残留

- `factor_lab/mining/eval_scope.py`：统一 `current_universe()` + `OVN_LABEL`
- `mining/evaluator.py`、`mining/backtest_runner.py`、
  `quanta/eval_agent.py`：不再写死 `csi300` / 隔日收益
- `factor_miner._init_qlib`：改走 `qlib_paths.qlib_init_kwargs`
- `quanta/config.py`：`BASE_FEATURES` 去掉 `$turn`；约束写明隔夜标签

### 还没清干净、会浪费本轮时间

全局因子池里仍有 5 个 **CSI300 时期** 的因子
（`mining_results/global_factor_pool.json`），其中一个表达式含 `$turn`
（`QA_PVCORR_REGIME_REVERT`）。方向注册表 35 个方向里，当前 pending 的
`dir_034` 是「换手率异常检测」——中证2000 的 `$turn` 全是 NaN，
这一向大概率空转。经验记忆也是 CSI300 口径。

本轮不要 `--accept` 旧池里的 CSI300 因子。
下一轮启动前应：清空或隔离 CSI300 池/方向，禁止换手率方向。

## 2026-09-12 代码审计 + 修复（验收数字作废）

### exec_lag=0 全树失效（9 处）

```python
int(cfg.get("exec_lag", 1) or 1)   # yaml 写 0，这里读成 1
```

`0 or 1` 求值成 1。9 处：`daily.py`×4、`paper_trader.py`×2、
`live_portfolio.py`×2、`reconcile.py`×1。

**上表所有验收数字作废。** Sharpe 0.87/0.98、超额 +40.8%、
tv25 p=0.003、unk40 p=0.008 —— 全部跑的是 `exec_lag=1`（T+1 收盘成交），
不是路线 B 的 14:50 竞价。标签是 `Ref($open,-1)/$close-1`（T 收盘→T+1 开盘），
而 exec_lag=1 在 **T+1 收盘**买入，整段隔夜跳已经错过。

与 `diagnose_horizon` 对不上：诊断说超额几乎全在第一晚（Top100 +44.5bp，
之后 7 日只剩约 +5bp），而回测吃不到第一晚却报 +40.8% 超额。
两个测量至少有一个描述的不是同一件事，重跑前不要引用。

其它后果：
- `daily auction` 的 `if lag != 0: return 2` 永远成立 —— 周一生产入口
  被自己挡住，还提示「先切 exec_lag=0」（配置本来就是 0）
- `cmd_signal` 的护栏只 `print` 不 `return`，警告完照样改写 pending
- `paper_trader` 打印原始值 0、执行用 1，**日志主动误导**

已修：抽出 `qlib_paths.parse_exec_lag()`，键存在且为 0 时走 None 判定。

### 修 exec_lag 会激活前视，所以加了护栏

预测 pkl 由 `run_rolling_benchmark` 在**完整日线**上生成，里面的 `$close`
是当天真实收盘价。exec_lag 真的变成 0 之后，回测**用它即将成交的那个价格
打分**，而实盘 14:45 拿不到。Alpha158 几乎每个因子都吃当天 `$close`。

这个策略的超额几乎全在隔夜那一跳，而「尾盘在拉升」与「隔夜继续跳」
高度相关 —— 正是最容易被这 15 分钟前视吃掉的部分。

`paper_trader.assert_no_close_lookahead()`：exec_lag=0 且截断日线目录为空
时**拒绝回测**并说明出路。实测三种情形判对（拒绝 / 显式放行 / exec_lag=1）。

**次序结论（2026-09-13 更正）：** 昨天写的是"补截断数据集是修 exec_lag 的
前置条件"。实测后不成立 —— 截断特征与完整特征秩相关 0.9984、Top100 重合
91.88%，前视量级只有约 8% 的选股差异。exec_lag=0 可以直接用，护栏已改为
告警而非拒绝。详见文末 2026-09-13 一节。

### 对账从未在本树跑通

`quality_score.pkl` 不存在 → 逐日订单对账直接 FileNotFoundError。
而 `adaptive_strategy: none`，本就不需要质量分，代码却无条件加载。
CLAUDE.md 写着「先跑这个再谈别的」的安全网是断的。

已修（按 `adaptive_strategy` 条件加载）。修后首次跑通：
**60 交易日 / 8 调仓日 / 0 分叉**，三项全过。

### `config.settings` 导入即崩

`stop_loss: null` → `float(_SIGNAL_CFG.get('stop_loss', 0.08))` 里键存在
且为 None，默认值不生效，`float(None)` 抛 TypeError。
`check_stop_loss()` 一被调用就炸（`daily signal`、飞书持仓日报都在这条路上）。
已修：`STOP_LOSS=None` 表示不设单票止损，`check_stop_loss` 直接返回空。

### 未修：topk=100 在 10 万本金下是价格筛选器

每坑位 1000 元，`allocate_buys` 的 `expensive_ratio=1.5` 把「1.5 倍预算
买不起一手」的剔掉。40 个真实调仓日实测：

| 敞口 | Top100 实际成交 | 买入票中位价 | 被剔票中位价 |
|---|---|---|---|
| 满仓 | 59.9 只（最少 28） | 7.99 元 | 24.70 元 |
| 0.40 | **18.4 只（最少 2）** | 4.58 元 | 14.77 元 |

候选整体中位价 12.28 元，全池 50% 分位 14.2 元。
**实际持仓比模型选的系统性便宜约 35%**，而低价在 A 股不是中性特征。

更麻烦：**敞口一变，持仓构成跟着变**（中位价 7.99 → 4.58）。
vol_target 本该只缩仓位大小、不该改选股。所以 tv25 与 unk40 的配对检验
**被混淆** —— 改善里有多少来自风险缩放、多少来自组合悄悄换成更便宜的票，
目前分不开。开局敞口下最少只持 2 只，那已经不是组合。

改动会移动绩效数字，需先定口径再动。

### 未修：持仓期与诊断口径差 3 倍

topk=100 / n_drop=20 / 8 日调仓，但实际只持约 60 只 → 每次换手 33%
→ 平均持有约 3 个调仓周期 ≈ **24 个交易日**，不是 8 天。
`diagnose_horizon` 测的是 8 日前向收益。

### 重跑顺序

1. ~~修 `or 1`~~ 已做
2. ~~修对账~~ 已做，0 分叉
3. 定 topk/资金口径（否则后续所有参数检验带同一个混淆）
4. 补截断数据集 → 用截断特征重训
5. 才能跑真正的 exec_lag=0 验收，重建上面那些表

## 2026-09-13 实测结论（两处更正前一日的判断）

### 更正一：前视幅度远小于我昨天的断言，护栏已从"拒绝"改为"告警"

昨天写的是"回测在用它当天要成交的收盘价打分，验收数字本身是乐观的"。
那是从「Alpha158 几乎每个因子都吃当天 `$close`」推断的，**没有实测**。

`python -m factor_lab._trunc_feat`（16 个调仓日 × 约 2000 只）：

    158 因子截面秩相关均值      0.9984
    KMID 秩相关                 0.9976
    ROC5 Top100 重叠            98.69%
    代理分数秩相关 截断 vs 完整   0.9935
    同一代理 Top100 截断 vs 完整  91.88%
    最不稳的因子 KLOW           0.9798

14:45 截断特征与完整日线**几乎同分布**，最终 Top100 约 92% 重合。
前视真实存在，但量级是"约 8% 的选股差异"，不是"整体乐观"。
据此硬拒绝全部验收不成比例 —— `assert_no_close_lookahead` 改为每次打印
量级并进报告，不再抛 SystemExit。

`python -m factor_lab._trunc_ic`（路线 B 的杀/走门禁，此前从未跑过）：

    IC 隔夜(从收盘)   0.2225
    IC 隔夜(从14:45)  0.1459
    14:45 vs 收盘价格秩相关  1.0000
    |最后15分钟涨跌|均值      0.2685%
    Top100 次日涨停率 0.69%，涨停票尾盘 -0.1373%

路线 B 是 **14:50 收盘竞价**成交，不是 14:45 成交，所以相关的是 0.2225（完好）。
秩相关 1.0000 + 尾盘几乎不动 → **隔夜 alpha 不是尾盘异动的产物**，门禁放行。

**由此：11.8 小时全量分钟下载按原定理由（补训练特征）不需要做。**
截断特征已够稳，脚本自己的结论是"下一步才是改标签并重训"。
真正待解的是**标签（隔夜）与持仓期（8 天）错配**，即文档原来的第 1 优先级。

### 更正二：topk 维持 100，我的"降到 30"建议被配对检验推翻

先按排名分桶测隔夜 alpha（82 个调仓日）：

| 排名段 | 隔夜超额 | 累计 topk | 隔夜超额 |
|---|---|---|---|
| 1-10 | **+268.3bp** | 10 | +268.3bp |
| 11-20 | +34.3 | 30 | +110.1 |
| 21-100 | +19~28 | 100 | +49.6 |
| 101-500 | +14.7~19.6 | | |

据此我判断"alpha 几乎全在前 10 名，topk=100 稀释 5.4 倍"，建议降到 30。
**配对检验推翻了它。**

第一次扫描还犯了一个方法错误：只扫 topk、`n_drop` 固定 20，
换手率从 20% 跳到 67%（topk=30）乃至 100%（topk=20），
测到的是换手成本不是 topk 效应。已给 `run_param_sweep` 加复合参数支持
（`--param topk+n_drop`）。

换手恒定后（8 相位配对，基线 100+20）：

| topk+n_drop | ΔSharpe | 配对 t | 胜出 | Δ回撤 | Δ超额 |
|---|---|---|---|---|---|
| 60+12 | -0.108 | -2.17 | 2/8 | -0.76% | -9.06% |
| 40+8 | -0.293 | -7.03 | 0/8 | -2.77% | -25.63% |
| 30+6 | -0.452 | -8.25 | **0/8** | -5.11% | **-38.36%** |

单调且显著。**topk 维持 100，不要改。**

排名分析为什么误导：
1. 它只看每档的**均值**，没看方差。Sharpe 取决于两者之比，集中化抬高均值
   但更抬高波动。
2. 它是"前 N 名等权"的理论组合，而真实组合受整手约束 —— topk=100 时
   每坑位 1000 元、价格上限 15 元，**实际持有的是前 100 名里最便宜的 60 只**。
   而这个策略的 alpha 恰恰在薄的低价票上（诊断：持仓 43.8% 落在最薄成交额档）。
   **即价格筛选意外地成了微盘倾斜，方向与 alpha 来源一致。**

这条对"做微盘股"这个目标很重要：现在的整手约束不是纯损害，它在做 alpha 的工作。
但它是**隐式**的 —— 随敞口漂移（0.4 敞口时上限降到 6 元），且与 vol_target 耦合。
要不要把它改成显式的股票池筛选，需要单独设计与检验，不能靠它"碰巧有效"。

注：该扫描跑在 `exec_lag=1` 下（无前视的干净口径）。标签是隔夜而 exec_lag=1
在 T+1 收盘买入、吃不到隔夜，所以这是保守口径；隔夜 alpha 集中度高于 8 日
alpha，理论上 exec_lag=0 会更有利于低 topk —— 但差距达 -38%，方向不可能反转。
配置已还原 exec_lag=0，对账 0 分叉。

### 其它已核实（不必重查）

- 挖掘锁机制正常：`_pid_alive` 三种情形判对，残留锁会被覆盖（昨晚 PID 32288
  的残留锁已清）
- 卖出候选"无分数"是**指数调出**导致：预测按时点成分股生成，被调出的持仓
  没有分数，排最前优先卖 —— 正确行为
- 300 只随机样本分钟数据已下（299/300 成功，1.02h）；`fetch_minute` 新增
  `--sample N`，因为 `--limit N` 取代码序前 N 个全是 sh600xxx 主板老票，
  做 IC 门禁会偏离薄票

## 2026-09-13 下半场：前视量级实测、冒烟套件、持仓期

### 前视值多少个百分点 —— 实测，不再插值

上午我写过"真值在两者之间、更靠近上面那个"。**那是没有依据的插值**：
用"选股只差 8%"推出"业绩影响也小"并不成立 —— 不重合的那批恰恰是尾盘在动
的票，而本策略赚的就是隔夜跳，它们可能携带不成比例的 alpha。

用落盘 booster（预测期 2026-07-01~09-11）做同模型、同股票、同日期的对照，
唯一差别是特征快照来自 14:45 还是收盘：

    7 个调仓日，Top30 / 约 213 只可选
    Top30 重合度      80.5%
    分数秩相关        0.9701
    隔夜超额  截断特征 +20.0bp
              完整特征 +21.4bp   <- 含前视
    前视把超额放大 1.07 倍，配对 t = 0.55

**即便选股差了 20%，业绩只差 7%，且统计上分不出差异。** 我担心的
"尾盘在动的票携带不成比例 alpha"在这个样本里没有出现。

样本很小（7 个调仓日、2.4 个月、213 只），`|t|=0.55` 的正确读法是
"分不出差异"，不是"证明没差异"。

含义：生产口径超额 +70.35%，按 1.07 倍折回约 +65%，仍远高于 exec_lag=1 的
+40.81%。那 30pp 的差主要来自**吃到隔夜**，不是前视。11.8 小时全量分钟下载
的紧迫性因此下降。

### 又一个"只有真跑才会暴露"的崩溃

`auction_infer._trunc_table` 对字符串列 `last_tod` 调 `.median()`，
新版 pandas 直接抛 TypeError。这行在 `score_day` 的**必经路径**上 ——
`daily auction` 一跑就崩。此前只做过空跑，空跑走不到那一步。已改 `.mode()`。

### smoke.py —— 冒烟套件

这两天查出的真缺陷几乎全是"跑起来"发现的，读代码得出的判断错了好几次。
所以补一个套件：每个生产入口用极小规模**真跑**一遍，只看退出码与异常栈。

与 reconcile 分工：

    reconcile  两条路径算出来的**结果**是否一致
    smoke      每条路径**还能不能跑**

8 个入口 + 3 项守卫自检，首跑 332s 全过：

    daily-status / reconcile / phase-test / param-sweep / gm-check
    auction-score(实盘 14:45 出分) / trunc-daily / fetch-minute
    validator(前视+死字段) / bar-check(量纲) / exec-lag(0 不被 or 吃掉)

守卫自检是必要的：守卫改坏了会**静默放行**，必须每次验证它们还在拦。
故意排除 5 个入口（refresh/place/factor_miner/persist_boosters/retrain），
理由写在 `EXCLUDED` 里。

建议：改任何调仓规则或数据管线后，先 `python smoke.py` 再 `python reconcile.py`。

### 持仓期：缩短反而更差，文档原判断需要修正

文档原写"Top100 隔夜 +44.5bp，之后 7 日只剩约 +5bp，后面在坐指数 beta"，
据此把"对齐持仓和标签"列为第 1 优先级。

先按排名分桶独立测了一次（82 个调仓日）：

    Top100  隔夜超额 +49.6bp    8 日超额 +74.3bp

**8 日超额高于隔夜超额**，即后 7 天不是白坐，还在继续赚。与文档的 +5bp 不符。

再用 8 相位配对直接扫（换手率保持恒定，`exec_lag=0`）：

| rebalance+n_drop | Sharpe 均值 | 最差相位 | 回撤 | 超额 |
|---|---|---|---|---|
| **8+20（基线）** | **1.240** | 1.011 | -18.42% | **+70.35%** |
| 4+10 | 1.147 | 1.083 | -20.84% | +62.06% |
| 2+5 | 1.150 | 1.086 | -22.63% | +65.38% |
| 1+3 | 1.068 | 1.068 | -21.79% | +52.96% |

配对比较（基线 8+20）：

| 候选 | ΔSharpe | 配对 t | 胜出 | Δ回撤 | Δ超额 |
|---|---|---|---|---|---|
| 4+10 | -0.093 | -1.65 | 3/8 | -2.42% | -8.29% |
| 2+5 | -0.090 | -1.43 | 2/8 | -4.21% | -4.97% |
| 1+3 | -0.172 | -2.83 | 1/8 | -3.37% | -17.39% |
| **1+20** | **-0.299** | **-4.91** | **0/8** | -4.43% | **-30.09%** |

**结论：维持 rebalance_every=8。** 但措辞要准确 ——

- 4+10 与 2+5 的 |t| < 2，**在噪声范围内，分不出差异**
- 只有日频（1+3 / 1+20）显著更差

所以正确说法是"**没有证据支持从 8 改成别的，而日频显著更差**"，
不是"轮动越快单调更差"。（我第一次写的正是后者 —— 在看到 t 值之前就
从均值下了单调结论，违反自律第 2 条。）

1+20 是"每天调仓、每天换 20 只"，即最大限度多吃隔夜。它超额掉 30 个
百分点、t=-4.91、0/8 胜出 —— **决定性地回答了"多吃隔夜值不值"：
换手成本远超隔夜收益。**

注：`rebalance_every=1` 时每天都是调仓日，8 个相位完全相同（所以最差=均值），
那一档实际只有 1 个独立样本。

一个方法论提醒：第一次扫 topk 时我只改 topk、没动耦合的 `n_drop`，
换手率从 20% 跳到 67~100%，测到的是换手成本而非 topk 效应。
`run_param_sweep` 已支持 `--param a+b` 复合扫描，**凡是耦合参数必须一起扫**。

### 因子池与挖掘方向已清理

- `QA_PVCORR_REGIME_REVERT` 依赖 `$turn` -> 隔离到
  `global_factor_pool.quarantined_20260913.json`。依据：直读 bin 实测
  `$turn` 与 `$isST` 在 cn_data_csi2000 上 **100% NaN**（15 只 / 25275 点）
- 余下 4 个补了 `universe` / `fields_used` 结构化字段（原先溯源只写在
  hypothesis 散文里）。它们都选自 CSI300 时期，未在中证2000 验证过
- `dir_034`（换手率异常检测）标为 `blocked`
- **根上的修法**：`mining/validator.py` 的字段合法性改为**按当前数据集实测
  可用性判定**，不再靠写死名单。`$turn` 表达式现在会被直接拒绝，换股票池或
  补字段后名单自动跟着变
- 准入用 `abs(rank_ic)` 是**对的**，不是 bug：负 IC 因子取反即正，
  且送进 LightGBM 后模型自己学符号

### daily status 现在会报字段可用性

    字段可用: close, open, high, low, volume, amount, vwap
    字段全 NaN(依赖它的过滤会静默失效): turn, isST

`$isST` 全 NaN 意味着 **ST 过滤在本数据集上不可能生效**。实测影响：
82 个调仓日里 Top100 出现 ST 股 5 次（占仓位日约 0.06%），ST 股平均排名分位
53.6%（≈随机），对绩效不重要。但 `price_limit` 会给 ST 创业板 ±19.5%
而实际是 ±10%。

**正确修法在数据层**（`data_hub/assemble.py:78` 显式写着 `"isST": np.nan`，
该从 Tushare 补），不是在策略层打补丁 —— 用今天的 ST 状态过滤历史是前视。

## 关键入口

| 层 | 入口 |
|---|---|
| 配置 | `config/signal_config.yaml` |
| 日更 | `python -m daily`（prefetch / auction / place / sync） |
| 验收 | `python -m factor_lab.run_phase_test` |
| 模拟盘 | `factor_lab/paper_trader.py` |
| 出分 | `factor_lab/auction_infer.py` |
| 挖掘 | `python -m factor_lab.factor_miner` |
| 诊断 | `python -m factor_lab.diagnose_horizon` |
| 路径 | `qlib_paths.py`、`data_hub/paths.py` |

周末/盘后不要跑 auction / place。下周一窗口：
14:31 prefetch → 14:45 auction → 14:50 place → 15:05 sync。
