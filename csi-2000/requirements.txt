# 安装: <python> -m pip install -r requirements.txt
#
# ⚠ Python 版本: 需 3.10~3.12。pyqlib 在 3.13 上没有可用发行版
#   (pip 报 "no matching distribution")，系统自带的 3.13 装不了。
#
# ⚠ 包名是 pyqlib 不是 qlib。2026-09-05 更正: 本文件原先写 "qlib"，
#   那是 PyPI 上的另一个包，照着装会得到一个装不上/装错的环境。
#   仓库根目录恰好也叫 qlib，`import qlib` 会被目录当成命名空间包冒充成功，
#   使"没装上"这件事很难被发现。

# --- 核心 ---
pyqlib==0.9.7          # 微软 Qlib
lightgbm>=4.6
pandas>=2.0
numpy>=1.24
scipy>=1.10
scikit-learn>=1.3
PyYAML>=6.0

# mlflow 3.x 废弃了文件系统追踪后端，而 qlib 0.9.7 默认用它。
# qlib_compat.py 会设置 MLFLOW_ALLOW_FILE_STORE=true 绕过，导入即生效。
mlflow>=3.0

# --- 数据源 ---
baostock               # 主数据源 (qlib_engine/data_setup.py)
akshare                # 新浪数据源 + 交易日历 + 新闻 (data_setup_sina.py /
                       # market_calendar.py / news_sentinel.py)
                       # 2026-09-05 补: 此前缺失，data_setup_sina 直接
                       # ModuleNotFoundError 退出并回落 baostock

# --- 飞书机器人 ---
lark-oapi
python-dotenv
redis                  # 可选: 临时文件存储。不可用时 TempFileStore 优雅降级到磁盘

# 2026-09-05 移除 faiss-cpu / onnxruntime / tokenizers / huggingface_hub:
# README 把它们列为"记忆系统(Faiss 向量检索)"依赖，但全库对这四个包
# 零引用 —— 该功能从未实现。装它们会白白拉进数百 MB 且拖慢环境搭建。

# --- 掘金量化仿真 (可选，只在接模拟盘时需要) ---
# 会把 protobuf 降到 3.20.3。实测 qlib 0.9.7 / lightgbm 4.6 / pandas 2.3 /
# lark_oapi 均不受影响。仅装 SDK 不够 —— 交易接口要求掘金终端常驻运行，
# 且账户接口只在 run(strategy_id=..., mode=MODE_LIVE) 启动的策略里返回数据。
gm                     # gm_bridge.py
