---
description: 当前工作只针对 CSI2000，不要改 CSI300 实盘
alwaysApply: true
---

# 只改 CSI2000

从 2026-09-10 起，本仓库里的改动、数据中台、回测和策略实验
**全部针对中证2000**，目录是 `csi-2000/`。

- 改代码、配置、数据写入、删除无用数据源：只动 `csi-2000/`。
- 不要改 `gy_q/trading_framework` 的实盘配置、持仓、定时任务、
  `cn_data_bs`。那是 CSI300 活系统，不是当前任务。
- 不要为了「给 300 留后路」在 `csi-2000` 里保留新浪/BaoStock 写入。
  中证2000 行情中台只走 Tushare。
- 唯一例外：正在跑的 15 分钟下载进程在原树缓存目录里，不要杀它；
  续传补丁可以改原树 `fetch_minute.py`，不要改 CSI300 策略配置。
- 不要把 CSI300 的 TopK、vol_target、止损、新浪刷新当约束。
- 架构要对齐多标的（配置里换 universe），但仍然禁止改 CSI300 活系统。
