# 复制为 .env 并填入真实值。.env 已在 .gitignore 中，不会进入版本控制。
#
# 注意: 2026-08-30 曾尝试把真实 .env 提交进 git，被 GitHub 的 secret scanning
# 拦截 (Lark Application Secret)。git 历史是永久的，密钥一旦进入历史，即便
# 日后删除文件也仍可从历史中还原。请始终只提交本模板。

# ============ 飞书应用凭证 (必填) ============
# 在 https://open.feishu.cn/app 创建企业自建应用后获取
FEISHU_APP_ID_1=cli_xxxxxxxx
FEISHU_APP_SECRET_1=xxxxxxxx

# 可选: 第二个应用
# FEISHU_APP_ID_2=cli_yyyyyyyy
# FEISHU_APP_SECRET_2=yyyyyyyy

# ============ 授权白名单 (必填) ============
# 逗号分隔的 open_id。留空则拒绝所有人。
#
# 为什么必须配: 机器人收到消息后会以 --dangerously-skip-permissions 调用
# Claude CLI，等于把本机的任意命令执行权交给发消息的人。
#
# 首次配置: 先启动机器人 -> 给它发条消息 -> 控制台会打印你的 open_id
#           -> 填到这里 -> 重启
FEISHU_ALLOWED_OPEN_IDS=

# 已知用户 (用于旧记忆迁移，可选)
# FEISHU_USER_OPEN_ID=ou_xxxxxxxx

# ============ Redis (可选) ============
# 不配置时 TempFileStore 会优雅降级: 临时文件留在磁盘、去重转内存
# REDIS_URL=redis://localhost:6379/0
TEMP_FILE_TTL=7776000

# ============ VPN 重启命令 (可选) ============
# 网络中断时尝试恢复。留空则跳过。
# macOS 示例: nohup /Applications/LetsVPN.app/Contents/MacOS/LetsVPN >/dev/null 2>&1 &
VPN_RESTART_CMD=

# ============ Claude CLI 路径 (可选) ============
# 默认由 cli_paths.py 自动探测 (PATH -> 常见安装位置)。探测失败时才需手动指定。
# CLAUDE_BIN=C:\Users\<you>\AppData\Roaming\npm\claude.cmd
